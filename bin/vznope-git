#!/usr/bin/env perl

use strict;
use warnings;
use VZNope;

my $ident = shift;
help() unless $ident;
VZNope->container_git($ident, @ARGV);

sub help {
    print do{ local $/; <DATA> };
    exit;
}

__DATA__

  usage:
    vzn git [options for git]

