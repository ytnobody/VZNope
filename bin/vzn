#!/bin/bash

bindir=$(dirname $0)
subcmd=$1 ; shift
APPROOT=$bindir/..
PERLROOT=$APPROOT/perl5/perl-5.18
PERLBINDIR=$PERLROOT/bin
CARTON=$PERLBINDIR/carton

if [ -z "$subcmd" ] ; then
    subcmd=help
fi

cd $APPROOT
$CARTON exec perl -Ilib -Ilocal/lib/perl5 $bindir/vznope-$subcmd $*
